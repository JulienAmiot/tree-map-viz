node_modules
dist
*.local
.DS_Store

# TypeScript build cache
*.tsbuildinfo

# Playwright + playwright-bdd artefacts
playwright-report/
test-results/
src/test/e2e/.features-gen/
playwright/.cache/

# Local env (never commit secrets)
.env
.env.*
!.env.example
!.env.sonar.example

# Local stdout captures from npm scripts (vitest.log, build.log, etc.)
*.log

# Vitest coverage output (consumed by SonarQube; never committed)
coverage/

# SonarQube scanner work directory (cache + intermediate reports)
.scannerwork/

# Local SonarQube credentials (§17.53) — see .env.sonar.example
.env.sonar
